# Pre-Flight Check: Release Workflow

## A. Traceability Matrix

- [x] Auto Patch Bump → Scenario: Successful auto-bump → `openspec/constitution.md` (new convention)
- [x] Version Sync → Scenario: Files in sync → `openspec/constitution.md` (convention handles both files)
- [x] Version Sync → Scenario: Files out of sync → `openspec/constitution.md` (sync-then-bump logic)
- [x] Manual Minor/Major → Scenario: Manual minor release → `openspec/constitution.md` (documented process)
- [x] Manual Minor/Major → Scenario: Optional GitHub Release → `openspec/constitution.md` (documented process)
- [x] Consumer Update → Scenario: Consumer updates → `openspec/specs/release-workflow/spec.md` → `/opsx:docs`
- [x] Consumer Update → Scenario: Update not detected → `openspec/specs/release-workflow/spec.md` → `/opsx:docs`
- [x] Skill Immutability → Scenario: Project-specific in constitution → `openspec/constitution.md` (new rule)
- [x] Archive Next Steps → Scenario: Next steps shown → `openspec/constitution.md` (convention)

## B. Gap Analysis

No gaps identified. All requirements have scenarios, edge cases cover invalid versions, missing fields, and fallback processes.

## C. Side-Effect Analysis

- **Constitution changes:** Replacing the manual bump convention (line 35) with auto-bump convention. Agents reading the old convention will now follow the new one — this is the desired effect, not a regression.
- **marketplace.json immediate fix:** Changing version from `1.0.0` to `1.0.3` aligns with plugin.json. No negative side effects — consumers benefit from consistent version reporting.
- **Archive output:** Adding next steps to the constitution convention means the archive output will now include version + next steps. This is additive, no existing output is removed.

## D. Constitution Check

**Yes, updates needed:**
1. Replace line 35 (manual version bump convention) with auto-bump convention
2. Add skill immutability rule under Architecture Rules
3. Add post-archive next steps guidance

No new technologies or architectural patterns introduced beyond what's already in the constitution.

## E. Duplication & Consistency

- **Auto-bump convention vs Version Sync requirement:** Complementary, not duplicated. Auto-bump defines when to bump; Version Sync defines that both files must stay aligned.
- **Consistency with existing specs:** No contradictions with `openspec/specs/change-workspace/spec.md`. The archive behavior is extended via constitution convention, not via spec modification.
- **README vs spec:** README gets a brief note; detailed docs are auto-generated by `/opsx:docs` from the spec. No duplication risk.

## F. Assumption Audit

| Assumption | Source | Rating |
|-----------|--------|--------|
| plugin.json uses top-level "version" field with semver | spec | Acceptable Risk — verified against current file |
| marketplace.json uses plugins[0].version | spec | Acceptable Risk — verified against current file |
| Consumers have gh CLI or GitHub web UI access | spec | Acceptable Risk — gh is optional, web UI always available |
| Archive skill reads constitution before execution | design | Acceptable Risk — standard behavior for all skills |

No blocking or needs-clarification assumptions.
