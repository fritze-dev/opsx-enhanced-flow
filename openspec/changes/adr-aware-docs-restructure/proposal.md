## Why

The `docs/README.md` is a monolithic 148-line auto-generated file where the Key Design Decisions table (26 ADRs) and Notable Trade-offs (~30 lines) consume nearly half the document and grow with every archived change. Separately, `/opsx:discover` reads constitution and specs for context but ignores existing ADRs — missing critical architectural context that explains constraints, patterns, and trade-offs relevant to new research.

## What Changes

- **README split:** `docs/README.md` becomes a compact hub/index (~40 lines) linking to `docs/architecture.md` (System Architecture, Tech Stack, Conventions) and `docs/decisions.md` (Key Design Decisions table, Notable Trade-offs). Capabilities tables remain in the README as the browsing entry point.
- **Per-file conditional regeneration:** Each output file gets its own trigger: `architecture.md` on constitution drift, `decisions.md` on ADR changes, `README.md` on capability changes or sub-file changes.
- **New templates:** `architecture.md` and `decisions.md` templates added to the schema.
- **Discover ADR awareness:** `/opsx:discover` Step 2 reads `docs/decisions.md` as decision index to identify thematically relevant ADRs, then deep-dives into specific `docs/decisions/adr-*.md` files as needed. Research template gains an optional "Related Decisions" section.
- **Schema instruction update:** Research artifact instruction includes ADR awareness guidance.
- **Supersedes ADR-009** (Consolidated README) — the new structure provides better separation of concerns while maintaining a clear entry point.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `architecture-docs`: README becomes hub; architecture content moves to `docs/architecture.md`; decisions content moves to `docs/decisions.md`; conditional regeneration becomes per-file; stale file cleanup updated
- `interactive-discovery`: Discovery reads existing ADRs for architectural context during research; research template gains "Related Decisions" section

### Consolidation Check

1. Existing specs reviewed: architecture-docs, decision-docs, user-docs, interactive-discovery, artifact-generation, artifact-pipeline, quality-gates, three-layer-architecture
2. Overlap assessment:
   - README restructuring touches `architecture-docs` (owns README generation) — Modified, not new
   - ADR table moved to `decisions.md` but still generated by the same `/opsx:docs` flow — `architecture-docs` already owns this, stays Modified
   - Discovery ADR awareness is a requirement change to `interactive-discovery` — Modified, not new
   - Considered modifying `decision-docs` but it only controls ADR file generation, not where they're listed — no change needed to its spec requirements
3. Merge assessment: N/A — no new capabilities proposed

## Impact

- **Specs:** Delta specs for `architecture-docs` and `interactive-discovery`
- **Skill:** `skills/docs/SKILL.md` Step 5 splits into sub-steps; `skills/discover/SKILL.md` Step 2 adds ADR reading
- **Schema:** `schema.yaml` research instruction updated; `templates/research.md` gains optional section
- **Templates:** New `templates/docs/architecture.md` and `templates/docs/decisions.md`; modified `templates/docs/readme.md`
- **Output files:** `docs/README.md` shrinks; `docs/architecture.md` and `docs/decisions.md` created

## Scope & Boundaries

**In scope:**
- Restructuring docs/README.md into hub + architecture.md + decisions.md
- Per-file conditional regeneration logic
- New templates for architecture.md and decisions.md
- ADR awareness in /opsx:discover skill and research template
- Schema instruction update for research artifact

**Out of scope:**
- No changes to capability doc generation (user-docs spec)
- No changes to ADR file generation (decision-docs spec requirements)
- No changes to the archive, sync, or apply workflows
- No changes to constitution content
