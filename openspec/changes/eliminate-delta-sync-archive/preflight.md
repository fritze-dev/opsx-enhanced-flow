# Preflight: Eliminate Delta-Specs, Sync & Archive

## A. Traceability Matrix

| Capability | Spec File | Requirements | Scenarios |
|------------|-----------|-------------|-----------|
| change-workspace | `specs/change-workspace/spec.md` | 6 (5 modified, 1 new: Active vs Completed) + 2 removed | 18 |
| artifact-pipeline | `specs/artifact-pipeline/spec.md` | 4 modified | 6 |
| artifact-generation | `specs/artifact-generation/spec.md` | 1 modified | 4 |
| quality-gates | `specs/quality-gates/spec.md` | 3 modified | 5 |
| task-implementation | `specs/task-implementation/spec.md` | 3 modified + 1 removed | 5 |
| release-workflow | `specs/release-workflow/spec.md` | 3 modified | 6 |
| user-docs | `specs/user-docs/spec.md` | 2 modified | 4 |
| interactive-discovery | `specs/interactive-discovery/spec.md` | 1 modified | 1 |

**Coverage:** All 8 capabilities from the proposal are traced to delta specs. The removed capability (spec-sync) has no delta spec — it will be deleted entirely during implementation.

## B. Gap Analysis

**Scenario coverage:** All modified requirements have at least one scenario. No requirements lack scenarios.

**Missing edge cases — none critical:**
- The "Active vs Completed Change Detection" requirement is new and well-covered with 3 scenarios.
- Lazy worktree cleanup covers the main paths (merged, unmerged, no gh CLI).

**Error handling gaps — none:**
- All skills have graceful degradation for missing files (proposal missing, tasks.md missing, etc.).

## C. Side-Effect Analysis

| Affected System | Risk | Assessment |
|----------------|------|------------|
| Existing 37 archived changes | Migration moves files; could break git blame | LOW — `git mv` preserves history; content unchanged |
| CHANGELOG.md | Changelog skill reads new paths; old entries must be preserved | LOW — skill preserves existing entries by design |
| docs/ generated documentation | Docs incremental detection mechanism changes | MEDIUM — new detection uses proposal Capabilities; requires thorough testing |
| Plugin consumers | No consumer-facing behavior changes | NONE — this is an internal workflow change |
| CI/CD (GitHub Actions) | Release workflow references archive in constitution | LOW — only constitution text changes, no CI config changes |
| README.md | Docs README references architecture with archive/sync | LOW — regenerated by `/opsx:docs` after implementation |

## D. Constitution Check

| Rule | Status |
|------|--------|
| Three-layer architecture | ✅ Maintained — constitution, workflow, skills layers unchanged |
| Skill immutability | ✅ Respected — removing skills and modifying generic behavior, not adding project-specific logic |
| No ADR references in specs | ✅ No ADR references in any delta spec |
| Review markers auto-resolved | ✅ No REVIEW markers in any artifact |
| Design review checkpoint | ✅ Checkpoint was held after design before this preflight |

**Constitution update needed:** Architecture Rules section references "Delta specs", "Archives", and "Sync" — must be updated during implementation.

## E. Duplication & Consistency

**Cross-spec consistency:** All 8 delta specs consistently use:
- `openspec/specs/<cap>/spec.md` for baseline spec references (not `openspec/changes/<name>/specs/`)
- `openspec/changes/YYYY-MM-DD-<name>/` for change directory references (not `archive/`)
- "completed changes" terminology (not "archived changes")
- tasks.md status for active/completed detection

**Terminology consistency:** "Archive" and "sync" terms are removed consistently across all delta specs. No spec references the removed `/opsx:sync` or `/opsx:archive` commands.

**No overlapping requirements:** Each delta spec modifies distinct requirements within its capability.

## F. Assumption Audit

| Assumption | Source | Risk Rating |
|------------|--------|-------------|
| System clock accuracy | change-workspace spec | Acceptable Risk — standard system dependency |
| Proposal completeness in old archives | design.md | Acceptable Risk — verified: all 37 archives have proposal.md |
| Single-developer workflow | design.md | Acceptable Risk — project is single-developer; team scenario addressed via git merge conflicts |
| gh CLI reliability | design.md | Acceptable Risk — fallback to `git branch -d` documented |
| File-existence-based completion | artifact-pipeline spec | Acceptable Risk — existing pattern, well-tested |
| Naming enforced by new skill | release-workflow, user-docs specs | Acceptable Risk — `/opsx:new` is the sole creation path |
| Proposal-spec traceability | user-docs spec | Needs Clarification — old archives may not have structured Capabilities sections |
| Specs stage edits committed | quality-gates spec | Acceptable Risk — post-artifact hook stages `openspec/specs/` |

**Clarification needed on "Proposal-spec traceability":** The early archives (e.g., `2026-03-02-initial-spec`) may have proposal formats that predate the current Capabilities section structure. The docs skill should handle missing or differently-structured Capabilities sections gracefully (fall back to full regeneration if parsing fails).

## G. Review Marker Audit

**ASSUMPTION markers:** 8 found across specs and design, all with visible text and rated above.

**REVIEW markers:** 0 found. No unresolved review markers.

## Verdict: PASS WITH WARNINGS

**Warnings (1):**
1. **Proposal-spec traceability for old archives:** Early archived changes may not have the current Capabilities section format. The docs skill's incremental detection should handle this gracefully — either by falling back to full regeneration or by skipping enrichment for changes without parseable Capabilities sections. Recommend adding a fallback clause to the docs skill implementation.
