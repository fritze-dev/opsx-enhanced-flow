# Pre-Flight Check: /opsx:docs-verify

## A. Traceability Matrix

- [x] **Requirement: Documentation Drift Verification**
  - Scenario: All documentation is in sync → Dimension A (capability docs), Dimension B (ADRs), Dimension C (README), Report generation (Step 6)
  - Scenario: Capability doc missing for a spec → Dimension A, CRITICAL classification
  - Scenario: Capability doc omits a requirement → Dimension A, WARNING classification
  - Scenario: README missing a capability → Dimension C, CRITICAL classification
  - Scenario: Stale ADR reference in README → Dimension C, WARNING classification
  - Scenario: Documentation directory does not exist → Dimension A graceful degradation
  - Scenario: No archived design decisions → Dimension B skip logic
  - Scenario: Manual ADR without design decision → Dimension B manual ADR recognition

All 8 scenarios trace to specific skill steps and components. Full coverage.

## B. Gap Analysis

No significant gaps identified:

- **Error handling:** Covered — graceful degradation for missing `docs/` directories, missing archives, missing README. Each scenario specifies behavior.
- **Empty states:** Covered — empty `docs/capabilities/`, no archives, no README all have explicit scenarios.
- **Edge cases in spec:** 5 edge cases defined (naming mismatch, multi-spec-to-one-doc, empty doc, custom README sections, concurrent regeneration). Reasonable coverage.
- **No input validation edge case:** The skill has no user input (no change name to select, no arguments) — it operates on the entire project state. No input validation needed.

## C. Side-Effect Analysis

| Area | Risk | Assessment |
|------|------|------------|
| Existing `quality-gates` spec | ADDED requirement increases spec size | LOW — spec grows from ~190 to ~290 lines, within 250-350 target |
| Existing `/opsx:preflight` skill | No impact | NONE — preflight is unchanged |
| Existing `/opsx:verify` skill | No impact | NONE — verify is unchanged |
| Existing `/opsx:docs` skill | No impact | NONE — docs generation is unchanged, docs-verify only reads |
| Skills directory | New `skills/docs-verify/` directory | LOW — additive only, follows existing pattern |
| Constitution | No updates needed | NONE — no new patterns or architecture changes introduced |
| WORKFLOW.md / templates | No changes needed | NONE — docs-verify is not a pipeline artifact, it's a standalone skill |

No regression risks identified. The change is purely additive (new skill + delta spec).

## D. Constitution Check

- **Skill immutability:** ✅ The new skill will be generic plugin code in `skills/docs-verify/SKILL.md`, not project-specific.
- **Three-layer architecture:** ✅ Skill reads WORKFLOW.md for prerequisite check, operates within the skills layer.
- **No ADR references in specs:** ✅ Delta spec does not reference any ADRs.
- **Commit conventions:** ✅ Will use imperative present tense with category prefix.
- **Baseline spec format:** ✅ Delta spec uses `## ADDED Requirements` (correct for new requirement on existing capability).

No constitution conflicts.

## E. Duplication & Consistency

- **No overlap with preflight:** Preflight checks specs/design consistency pre-implementation. docs-verify checks docs vs specs post-generation. Different inputs, different timing, different outputs.
- **No overlap with verify:** Verify checks code vs specs/tasks post-implementation. docs-verify checks docs vs specs. Different targets.
- **No overlap with docs:** Docs generates documentation. docs-verify verifies generated documentation. Complementary, not duplicative.
- **Severity terminology:** Spec uses CRITICAL/WARNING/INFO. Design uses the same. Consistent with verify's CRITICAL/WARNING/SUGGESTION pattern (INFO replaces SUGGESTION for observational findings — documented in design Decision row).
- **Verdict terminology:** Spec and design both use CLEAN/DRIFTED/OUT OF SYNC. Consistent.
- **Assumption markers:** Spec and design share 3 identical assumptions with matching tags. Consistent.

No contradictions found.

## F. Assumption Audit

| # | Assumption | Source | Rating | Notes |
|---|-----------|--------|--------|-------|
| 1 | Capability docs use `<capability-name>.md` naming convention | spec + design | Acceptable Risk | Enforced by `/opsx:docs` generation; edge case for naming mismatch covered in spec |
| 2 | README uses parseable Markdown table format | spec + design | Acceptable Risk | Generated by `/opsx:docs` using a template; format is predictable |
| 3 | Archived design.md Decisions tables use consistent Markdown table format | spec + design | Acceptable Risk | Same assumption used by `/opsx:docs` ADR generation (Step 4 skip rule); proven in practice |

All assumptions have visible text before the HTML comment tag. All rated Acceptable Risk — they rely on conventions enforced by the existing `/opsx:docs` skill.

## G. Review Marker Audit

No `<!-- REVIEW -->` or `<!-- REVIEW: ... -->` markers found in any change artifacts.

---

## Verdict: **PASS**

- 0 blockers
- 0 warnings
- All scenarios traced to components
- No gaps, no side-effect risks, no constitution conflicts, no duplication
- All assumptions rated Acceptable Risk
- No REVIEW markers
