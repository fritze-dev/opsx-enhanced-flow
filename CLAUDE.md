# Project Rules

## Workflow

All changes to this project MUST go through the OpenSpec flow (`/opsx:workflow propose`, `/opsx:workflow apply`, etc.). Never edit specs, skills, templates, or docs directly — always create a change first.
